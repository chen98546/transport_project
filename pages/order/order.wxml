<van-tabs active="{{ status }}" type="card" bind:change="changTabEv" color="#83a4ff">
    <van-tab wx:for="{{tabList}}" wx:key="index" title="{{item.title}}">
        <OrderCard orderInfo="{{orderInfoList}}"></OrderCard>
    </van-tab>
</van-tabs>